[package]
name = "librepods-ipc"
version = "0.1.0"
edition = "2021"
description = "Shared IPC protocol (Command/Event types) between librepodsd and the LibrePods UIs."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
